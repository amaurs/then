## Iteration 1 - Consolidate file extensions (.jsx → .tsx, .js → .ts)
- Renamed 17 files: index.jsx, Scaffold.jsx, Home.jsx, Hooks.jsx, ThemeContext.jsx, Board.jsx, Menu.jsx, Slider.jsx, NotFound.jsx, ProtectedRoute.jsx, Presentation.jsx, GoogleAnalytics.jsx, Loader.js, util.js, rl/Board.jsx, rl/util.jsx, rl/electronicLife.jsx
- Updated all imports across ~48 files to remove explicit .jsx/.tsx/.js extensions (Vite resolves automatically)
- Updated index.html entry point from index.jsx to index.tsx
- Vite dev server returns 200 OK, no module resolution errors from the rename
- Pre-existing TS errors remain (unused vars, .md module declarations, react-ga types) — not related to this task
- Note: rl/*.js files (Game.js, Arrows.js, agent.js, etc.) and registerServiceWorker.js, MaskPass.js, AnaglyphSVGRenderer.js, Home.test.js were NOT renamed — they are pure JS without JSX/TSX content or are legacy code in rl/ subdirectory
- Next step needed: Task 2 — Deduplicate utility code (merge util.ts and tools.ts into single utils.ts)

## Iteration 2 - Deduplicate utility code (merge util.ts + tools.ts → utils.ts)
- Created `src/utils.ts` combining all exports from `util.ts` and `tools.ts`
- Deleted `src/util.ts` and `src/tools.ts`
- Updated 12 files with import path changes: Board.tsx, Scaffold.tsx, Animation.tsx, Distrito.tsx, Conway.tsx, Stereo.tsx, Quilt.tsx, Dense.tsx, Voronoi.tsx, Colors.tsx, Loom.tsx
- Consolidated Distrito.tsx's two separate `../../tools` imports into one
- Left `rl/util.tsx` untouched — it's a separate module with RL-specific components
- No new TypeScript errors introduced; all pre-existing errors remain
- Vite dev server returns 200 OK
- Next step needed: Task 3 — Fix Calendar type safety (change `photos` from `any` to `Map<string, number>`)

## Iteration 3 - Fix Calendar type safety
- Changed `useState<any>()` to `useState<Map<string, number>>()` for `photos` state
- Added `photos === undefined` to the loading guard (alongside existing `start === undefined` check) to fix TS2322 type mismatch
- No new TypeScript errors introduced; resolved the `Type 'Map<string, number> | undefined' is not assignable to type 'Map<string, number>'` error
- Pre-existing errors remain (unused React import, unused `e` param, `user` type from useAuth)
- Vite dev server returns 200 OK
- Next step needed: Task 4 — Audit and remove unused dependencies

## Iteration 4 - Audit and remove unused dependencies
- Searched codebase for usage of suspect packages:
  - `@tensorflow-models/blazeface` + `@tensorflow/tfjs` — USED in Nostalgia.tsx (kept)
  - `react-redux` — NOT used anywhere → removed
  - `history` — NOT used anywhere → removed
  - `react-beautiful-dnd` — USED in Names.tsx (kept)
- Ran `npm uninstall react-redux history --legacy-peer-deps` (peer dep conflict with @types/node required --legacy-peer-deps)
- Fixed 2 stale imports from iteration 2 that were missed: Quilt.tsx and Voronoi.tsx still had `../../util.js` instead of `../../utils`
- Build passes (`vite build` succeeds), dev server returns 200
- Next step needed: Task 5 — Remove remaining CRA artifacts (evaluate registerServiceWorker.js)

## Iteration 5 - Create CalendarContext to share sorted dates list with Album
- Created `src/CalendarContext.tsx` with `CalendarProvider` and `useCalendarDates` hook
- Context stores `sortedDates: string[]` — the sorted keys from the photos map
- Wrapped `CalendarProvider` around the blog routes in `index.tsx` (above both `/calendar` and `/calendar/:year/:month/:day`)
- Updated `Calendar.tsx` to call `setSortedDates([...photosMap.keys()].sort())` after fetching calendar data
- Build passes (vite build succeeds)
- Key learning: Can't use per-route providers since Calendar and Album are sibling routes — need shared ancestor. Route state would be lost on direct URL access, so context + fallback fetch is the right approach.
- Next step needed: Refactor Album.tsx to use infinite scroll — maintain ordered day sections, detect scroll position, fetch adjacent days
